# Introduction

En este artículo, se profundizaran y mencionaran algunos conceptos matemáticos que subyacen a los algoritmos y procesos. La idea de esta sección es funcionar como una CheatSheet sobre simbolos y operaciones aritméticas.

----
# Basic Arithmetic Operations

-  **Multiplication** (`*`): El operador de multiplicación (`*`) representa el producto de dos o más números o expresiones. Por ejemplo:
$$
2 * 8 = 16
$$
-  **Division** (`/`): El operador de división (`/`) indica la división de un número o una expresión entre otro. Por ejemplo:
$$
40 / 2 = 20
$$
-  **Addition** (`+`): El operador de suma (`+`) representa la suma de dos o más numeros o expresiones. Por ejemplo:
$$
70 + 8 = 78
$$
- **Substraction** (`-`): El operador de resta (`-`) representa la diferencia entre dos o más números o expresiones. Por ejemplo:
$$
90 - 3 = 87
$$

----
# Algebraic Notations

- **Subscript Notation** (`x_t`): La notación con subíndice (`x_t`) representa una variable indexada por `t`, que suele indicar un paso temporal específico o un estado concreto en un secuencia.
$$
x_t = q(x_t \mid x_{t-2})
$$
	Esta notación se utiliza habitualmente en secuencias y series temporales, donde cada `x_t` representa el valro de `x` en el instante `t`.
-  **Superscript Notation** (`x^n`): La notación con superíndice (`x^n`) se utiliza para indicar exponentes o potencias. Por ejemplo:
$$
x^2 = x * x
$$

	Esta notación se utiliza en expresiones polinómicas y funciones exponenciales.

- **Norm** (`||...||`): La norma (`||...||`) mide el tamaño o la longitud de un vector. La norma más habitual es la norma euclidiana, que se calcula de la siguiente manera:
$$
\|v\| = \sqrt{v_1^2 + v_2^2 + \cdots + v_n^2}
$$
	- Otras normas son la norma `L1` (Distancia de Manhattan):
$$
\|v\|_1 = |v_1| + |v_2| + \cdots + |v_n|
$$

	- Y la norma `L∞` (Valor absoluto máximo):
$$
\|v\|_\infty = \max(|v_1|, |v_2|, \ldots, |v_n|)
$$

	Las normas se utilizan en diversas aplicaciones, como la medición de la distancia entre vectores, la regularización de modelos para evitar el sobreajuste y la normalización de datos.
- **Summation Symbol** (`Σ`): El símbolo de sumatoriaq (`Σ`) indica la suma de una sucesión de términos. Por ejemplo: 
$$
\sum_{i=1}^{n} a
$$

	Este representa la suma de los términos `a_1`, `a_2`, .... `a_n`. La suma se utiliza en muchas fórmulas matemáticas, entre ellas el cálculo de medidas, varianzas y series.

----
# Logarithms and Exponentials

 - **Logarithm Base 2** (`log2(x)`): El logaritmo en base 2 (`log2(x)`) es el logaritmo de x en base 2, que se utiliza a menudo en la teoria de la información para medir la entropía. Por ejemplo:
$$
\log_2(8) = 3
$$

	Los logaritmos se utilizan en la teoría de la información, la criptografía y los algoritmos por sus propiedades a la hora de reducir números grandes y gestionar el crecimiento exponencial.
	
- **Natural Logarithm** (`ln(x)`): El logaritmo natural (`ln(x)`) es el logaritmo de x en base e (El número de Euler). Por ejemplo:
$$
ln(e^2) = 2
$$
	Debido a su naturaleza suave y continua, el logaritmo natural se utiliza ampliamente en cálculo, ecuaciones diferenciales y teoría de la probabilidad.

- **Exponential Function** (Base 2) (`2^x`): La función exponencial (De base 2) (`2^x`) representa 2 elevado a la x, y se utiliza a menudo en sistemas binarios y métricas de información. Por ejemplo:
$$
2^3 = 8
$$


----
# Matrix and Vector Operations

- **Matrix-Vector Multiplication** (`A * v`): La multiplicación de una matriz por un vector (`A * v`) se refiere al producto de una matriz A y un vector V. Por ejemplo:
$$
A * v = [ [1, 2], [3, 4] ] * [5, 6] = [17, 39]
$$

> Esta operación es fundamental en álgebra lineal y es utilziada en varias aplicaciones, incluyendo la transformación de vectores, la resolución de sistemas de ecuaciones lineales y las redes neuronales.

- **Matrix-Matrix Multiplication** (`A * B`): La multiplicación de una matris por otra matriz (`A * B`) denota el producto de dos matrices (A y B). Por ejemplo:
$$
A * B = [ [1, 2], [3, 4] ] * [ [5, 6], [7, 8] ] = [ [19, 22], [43, 50] ]
$$

> Esta operación se utiliza en transformaciones lineales, en la resolución de sistemas de ecuaciones lineales y en el Deep Learning para las operaciones entre capas.

- **Transpose** (`A^T`): La transpuesta de una matriz A se denota como `A^T` e intercambia las filas y las columnas de A. Por ejemplo:
$$
\begin{gathered}
A = [ [1, 2], [3, 4] ]\\ A^T = [ [1, 3], [2, 4] ]
\end{gathered}
$$
> La transposición se utiliza en diversas operaciones con matrices, como el cálculo del producto escalar y la preparación de datos para determinados algoritmos.

- **Inverse** (`A^{-1}`): La inversa de una matriz A se denota como `A^{-1}` y es la matriz que, al multiplicarse por A, da como resultado la matriz indentidad. Por ejemplo:
$$
\begin{gathered}
A = [ [1, 2], [3, 4] ] \\ A^{-1} = [ [-2, 1], [1.5, -0.5] ]
\end{gathered}
$$
> La inversa se utiliza para resolver sistemas de ecuaciones lineales, invertir transformaciones y resolver diversos problemas de optimización.

- **Determinant** (`det(A)`): El determinante de una matriz cuadrada A es un valor escalar que se puede calcular y que se utiliza en diversas operaciones matriciales. Por ejemplo:
$$
\begin{gathered}
A = [ [1, 2], [3, 4] ] \\ det(A) = 1 * 4 - 2 * 3 = -2
\end{gathered}
$$

> El determinante permite determinar si una matris es invertible (Determinante distinto de 0) al calcular volúmenes, áreas y transformaciones geométricas.

- **Trace** (`tr(A)`): La traza de una matriz cuadrada A es la suma de los elementos de la diagonal principal. Por ejemplo:
$$
\begin{gathered}
A = [ [1, 2], [3, 4] ] \\ tr(A) = 1 + 4 = 5
\end{gathered}
$$
> La traza se utiliza en diversas propiedades de la matriz y en el cálculo de los valores propios.

----
# Set Theory

- **Carinality** (`|S|`): La cardinalidad representa el número de elementos de un conjunto S. Por ejemplo:
$$
\begin{gathered}
S = \{1, 2, 3, 4, 5\} \\ |S|=5
\end{gathered}
$$
	La cardinalidad se utiliza para contar elementos, realizar cálculos de probabilidad y resolver diversos problemas combinados.

- **Union** (`U`): La union de dos conjuntos (A y B) es el conjunto de todos los elementos que pertenecen a A, a B o a ambos. Por ejemplo:
$$
\begin{gathered}
A = \{1, 2, 3\}, B = \{3, 4, 5\} \\ AUB = \{1, 2, 3, 4, 5\}
\end{gathered}
$$
	La union es utilizada para combinar conjuntos, union de datos y en varios operaciones de conjuntos.

- **Intersection** (`∩`): La intersección de dos conjuntos A y B es el conjunto de todos los elementos que pertenecen tanto a A como a B. Por ejemplo:
$$
\begin{gathered}
A = \{1, 2, 3\}, B = \{3, 4, 5\} \\ A∩B = \{3\}
\end{gathered}
$$
	La intersección se utiliza para encontrar elementos en comun, filtrar datos y otras varias operaciones de conjuntos.

- **Complement** (`A^c`): El complemento de un conjunto A es el conjunto de todos los elementos que no pertenecen a A. Por ejemplo:
$$
\begin{gathered}
U = \{1, 2, 3, 4, 5\}, A = \{1, 2, 3\} \\ A^c = \{4, 5\}
\end{gathered}
$$
	El complemento es utilizado en operaciones con conjuntos, cálculos de probabilidad y diversas operaciones lógicas.

---
# Comparison Operators

- **Greater Than or Equal To** (`>=`): El operador `>=~`indica que el valor en la izquierda es o más grande o igual al valor de la derecha. ($a >= b$)
- **Less Than or Equal To** (`<=`): El operador `<=` indica que el valor en la izquierda es o más pequeño o igual al valor de la derecha. ($a <= b$)
- **Equality** (`==`): El operador `==`valida si dos valores son iguales. ($a == b$)
- **Inequality** (`!=`): El operador `!=`verifica si dos valores son diferentes. ($a != b$)

---
# Eigenvalues and Scalars

- **Lambda** (*Eigenvalue*) (`λ`): El símbolo lambda suele representar un valor propio en álgebra lineal o un parámetro escalar en ecuaciones. Por ejemplo:
$$
\begin{gathered}
A * v = λ * v, where λ = 3
\end{gathered}
$$
	Los valores propios se utilizan para comprender el comportamiento de las transformaciones lineales, el análisis de componentes principales (ACP) y diversos problemas de optimización.

- **Eigenvector**: Un vector propio es un vector distinto de cero que, al múltiplicarse por una matriz, da como resultado un múltiplo escalar de sí mismo. El escalar es el valor propio. Por ejemplo:
$$
A * v = λ * v
$$

	Los vectores propios se utilizan para comprender las direcciones de máxima varianza en los datos, en técnicas de reducción de dimensionalidad como el PCA y en diversos algoritmos de aprendizaje automático.

---
# Functions and Probability

- **Function Notation** (`f(x)`): La notación funcional representa a una función `f` aplicada a un input `x`. Por ejemplo:
$$
f(x) = x^2 + 2x + 1
$$
	La notación funcional se utiliza para definir relaciones matemáticas, modelar fenómenos del mundo real y en diversos algoritmos.

- **Conditional Probability Distribution** (`P(x | y)`): La distribución de probabilidad condicional se refiere a la distribución de probabilidad de `x` dada `y`. Por ejemplo:
$$
P(Output | Input)
$$
	Las probabilidades condicionales se utilizan en la inferencia bayesiana, en la toma de decisiones en condiciones de incertidumbre y en diversos modelos probabilísticos.

- **Expectation Operator** (`E[...]`):  El operador de expectativa / esperanza representa el valor esperador o la media de una variable aleatoria con respecto a su distribución de probabilidad. Por ejemplo:
$$
E[X] = \sum\ x_i\ P(x_i)
$$
	La Expectation se utiliza para calcular la media, en la toma de decisiones en condiciones de incertidumbre y en diversos modelos estadísticos.

- **Variance** (`Var(X)`): La varianza midel la dipersión de una variable aleatoria `X` en torno a su media. Se calcula de la siguiente manera: 
$$
Var(X) = E[(X - E[X])^2]
$$
	La varianza es utilizada para comprender la dispersión de los datos, evaluar el riesgo y aplicar diversos modelos estadísticos.

- **Standard Deviation** (`σ(X)`):  La desviación estándar es la raíz cuadrada de la varianza y proporciona una medida de la dispersión de una variable aleatoria. Por ejemplo: 
$$
σ(X) = \sqrt(Var(X))
$$
	La deseviación estándar se utiliza para comprender la dispersión de los datos, evaluar el riesgo y aplicar diversos modelos estadísticos.

- **Covariance** (`Cov(X, Y`): La covarianza mide cómo varían dos variables aleatorias `X` e `Y`. Se calcula de la siguiente manera:
$$
Cov(X, Y) = E[(X - E[X])(Y - E[Y])]
$$
	La covarianza se utiliza para comprender la relación entre dos variables, la optimización de carteras y diversos modelos estadísticos.

- **Correlation** (`ρ(X, Y`): La correlación es una medida de covarianza normalizada, que oscila entre -1 y 1. Indica la intensidad y la dirección de la relación lineal entre dos variables aleatorias. Por ejemplo:
$$
ρ(X, Y) = Cov(X, Y) / (σ(X) * σ(Y))
$$
	La correlación se utiliza para comprender la relación lineal entre variables en el análisis de datos y en diversos modelos estadísticos.
	
