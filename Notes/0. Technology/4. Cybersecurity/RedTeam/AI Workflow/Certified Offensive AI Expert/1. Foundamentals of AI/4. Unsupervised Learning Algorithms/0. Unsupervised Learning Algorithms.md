# Unsupervised Learning Algorithms

Los **Algoritmos de Aprendizaje no supervisado** exploran *datos sin etiquetar*, y su objetivo no es predecir un resultado concreto, sino *descubrir patrones, estructuars y relaciones ocultas* en los datos. A diferencia del [[0. Supervised Learning Algorithms]], en el que el algoritmo aprende a partir de ejemplos etiquetados, el aprendizaje no supervisado funciona sin la orientación de etiquetas predefinidias o "respuestas correctas".

> Podemos pensarlo como en explorar una nueva ciudad sin mapa. OBservariamos el entorno, identificariamos puntos de referencia y nos fijariamos en cómo se conectan las diferentes zonas. Del mismo modo, los algoritmos de aprendizaje no supervisado analizan las características inherentes de los datos para descubrir estructuras y patrones ocultos. 

----
# How Unsupervised Learning Works

Los **Unsupervised Learning Altorithms** identifican simulitudes, diferencias y aptrones en los datos. Pueden agrupar puntos de datos similares, reducir el número de variables conservando la información esencial o identificar puntos de datos inusuales que se desvían de la norma.

Estos algorimtos resultan valosos para tareas en las que los datos etiquetados osn escasos, costosos o no están disponibles. Nos p ermiten obtener información sobre la estructura y la organización subyacentes de los datos, incluos sin conocer los resultados o las etiquetas específicas.

Los probelmas de aprendizaje no supervisado pueden clasificarse a grandes rasgos en:

1. **Clustering**: Agrupar puntos de datos similares en función de sus características. Es como organizar una colección de libros por género o agrupar a los clientes según su comportamiento de compra.

2. **Dimesionality Reduction**: Reducir el número de variables (Características) de los datos conservando la información esencial. Es análogo a resumir un docuemnto extenso en un resumen conciso o comprimir una imagen sin perder sus detalles importantes.

3. **Anomaly Detection**: Identificar puntos de datos inusuales que se desvian significativamente de la norma. Es como detectar un billete falso entre una pila de billetes auténticos o detectar transacciones fraudulentas con tarjetas de crédito.

----
# Crucial Conepts in Unsupervised Learning

Para realmente entender el **aprendizaje sin supervisión**, es crucial comprender algunos conceptos básicos.
#### Unlabeled Data

El concepto básico más crucial son **los datos sin etiquetar**. A diferencia del aprendizaje supervisado ([[0. Supervised Learning Algorithms]]), en el que los puntos de datos vienen acompañados de etiquetas o varaibels sobjetivo correspondientes, los datos sin etiquetar carecen de estos resultados predefinidos. El algoritmo debe basarse únicamente en las características inherentes de los datos y en las características de entrada para descubrir patrones y relaciones. 

> Pensemos como si se tratara de analizar una colección de fotografías sin pies de fotos ni descripciones. Aunque no conozcamos el contexto específico de cada foto, podríamos agrupar las fotos similares basándonos en características visuales como el color, la composición, el tema, etc.
> 
#### Similarity Measures

Muchos algoritmos de aprendizaje no supervisado se basan en cuantificar la similitud o la diferencia entre puntos de datos. Las medidas de similitud calculan en qué medida dos puntos de datos son parecidos o deiferentes en fu nción de sus características. Entre las medidas más comunes se inlcuyen:

- **Euclidean Distance**: Mide la distancia en lí9nea recta entre dos puntos en un espacio multidimensional.

-  **Cosine Similarity**: Mide el ágnulo entre dos vectores, que representan puntos de datos, y los valores más altos indican una mayor similitud.

- **Mahattan Distance**:  Calcula la distancia entre dos puntos sumando las diferencias absolutas de sus coordenadas.

La elección de la medida de similitud depende de la naturaleza de los datos y del algoritmo específico que se utilice.

#### Clustering Tendency 

La **Tendencia a la agrupación** (**Clustering Tendency**) se refiere a la propensión inherente de los datos a formar agrupaciones o grupos. Antes de aplicar algoritmos de agrupación, es fundamental avaluar si los datos muestran una tendencia natural a agruparse. Si los datos están distribuidos de manera uniforme y no presentan agrupaciones inherentes, es posible que los algoritmos de agrupación no arrojen resultados significativos. 


#### Cluster Validity

Es fundamental evaluar la calidad y la relevancia de los clústeres generados por un algoritmo de agrupamiento. La validez de los clústers implica evaluar métricas como:

- **Cohesion**: Mide el grado de similitud entre los puntos de datos dentro de un clúster. Una mayor cohesión indica un clúster más compacto y bien definido.
- **Separation**: Midel el grado de diferencia entre los distintos clústers. Una mayor separación indica clústers más diferenciados y bien separados.

Diversos índices de validez de los clústers, como la puntuación de silueta y el índice de Davies-Bouldin, cuantifican estos aspectos y ayudan a determinar el número óptimo de clústers.

#### Dimensionality

La **Dimensionalidad** se refiere al número de características o variables que contienen los datos. una dimensionalidad elevada puede plantear dificultades para algunos algoritmos de aprendizaje no supervisado, ay que aumental a complejidad computacional y puede dar lugar a la *curse of dimensionality*, donde los datos se vuelven dispersos y las distancias entre los puntos pierden significado.

#### Intrinsic Dimensionality
La **Dimensionalidad Intrínseca** de los datos representa su dimensionalidad inherente o subyacente, que puede ser inferior al número real de características. Refleja la información esencial contenida e nlos datos. Las técnicas de reducción de dimensionalidad tienen como objetivo reducir el número de características conservado al mismo tiempo esta dimensionalidad intrínseca.

#### Anomaly
Una **Anomalía** es un dato que se desvía significativamente de la norma o del patrón esperado en los datos. Las anomlaías pueden representar sucesos inusuales, errores oa ctividades fraudulentas. La dtección de anomalías es fundamental en diveras aplicaciones, como la detección de fraudes, la seguridad de redes y la supervisión de sistemas.

#### Outlier
Un **valor atípico** es un punto de datos que se encuentra muy alejado de la mayoría de los demás puntos de datos. Aunque es similar a una anomalía, el término **Outlier** se utilzia a manudo en un sentido más amplio. Los valores atípicos pueden indicar errores en la recopilación de datos, observaciones inusuales o patrones potencialmente interestantes.

#### Feature Scaling

El escalado de características es esencial en el aprendizaje no supervisado para garantizar que todas las características contribuyan por igual a los cálculos de distancia y a otros cálculos. Entre las técnicas más comunes se incluyen;

- **Min-Max Scaling**: Escala la características a un rango fijo.
- **Standarization** (**Z-score normalization**): Transforma las características para que tengan una media de cero y una varianza de una unidad.