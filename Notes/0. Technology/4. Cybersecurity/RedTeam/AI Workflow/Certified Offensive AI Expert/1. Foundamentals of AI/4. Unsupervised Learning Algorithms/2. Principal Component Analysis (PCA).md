#  PCA - Principal Component Analysis

El **Principal Component Analysis** (*PCA* - *Análisis de componentes principales*) es una *técnica de reducción de dimensionalidad que transforma datos de alta dimensionalidad en una representación de menor dimensionalidad*, conservando el mismo tiempo la mayor cantidad posible de información original. Para ello, identifica los componentes principales y nuevas variables que son combinaciones lineales de las características originales y capturan la máxima varianza de los datos. El PCA se utiliza ampliamente para la extracción de características, la visualización de datos y la reducción de ruido.

![[Pasted image 20260921073947.png]]

> Por ejemplo, en el procesamiento de imágenes, el PCA puede reducir la dimensionalidad de los datos de imagen identificando los componentes principales que capturan las características más importante de las imágenes, como los bordes, las texturas y las formas.

Pensemos en esto como en encontrar las "direcciones" más importantes en los datos. Imaginemos un diagrama de dispersión de puntos de datos. El PCA encuentra las líneas que mejor capturan la dispersión de los datos. Estas líneas representan los componentes principales.

![[Pasted image 20260921181800.png]]

Pensemos en una base de datos de imágenes faciales. El PCA puede utilizarse para identificar los componentes principales que captan las variaciones más significativas en los rasgos facilaes, como la forma de los ojos, el tamaño de la nariz y la anchura de la boca. Al proyectar las imágenes faciales en un espacio de menor dimensión definido por estos componentes principales, podemos buscar de forma eficiente rostros similares.

Hay tres conceptos clave en el PCA:

- **Variance** / **Varianza**: La varianza mide la dispersión de los puntos de datos alrededor de la media. El objetivo del PCA es encontrar los componentes principales que maximicen la varianza, captando así la información más significativa de los datos.

- **Covariance** / **Covarianza**: La covarianza mide la relación entre dos variables. El PCA tiene en cuenta la covarianza entre diferentes rasgos para identificar las direcciones de máxima varianza. 

- **Eigenvectors**: Los vectores propios representan las direcciones de los componentes principales, y los valores propios representan la cantidad de varianza explicada por cada componente principal.

El algoritmo PCA sigue los siguientes pasos:

1. **Standardize the data**: Restar la media y dividir por la desviación estándar de cada característica para garantizar que todas las características tengan la misma escala.
2. **Calculate the covariance matrix**: Calcular la matrix la covarianza de los datos estandarizados, que representa las relaciones entre las diferentes características.
3. **Compute the eigenvectors and eigenvalues**: Determinar los vectores propios y los valores propios de la matriz de covarianza. Los vectores propios representan las direcciones de los componentes principales, y los valores propios representan la cantidad de varianza explicada por cada componente principal.
4. **Sort the eigenvectors**: Ordena los vectores propios en orden descendente según sus valores propios correspondientes. Los vectores propios con los valores propios más altos capturan la mayor parte de la varianza de los datos.
5. **Select the principal components**: Elige los primeros k vectores propios, donde k es el número deseado de dimensiones en la representación reducida.
6. **Transform the data**: Proyecta los datos originales sobre los componentes principales seleccionados para obtener la representación de menor dimensión.

Siguiendo estos pasos, PCA puede efectivamente reducir la dimensionalidad del set de datos, conservando al mismo tiempo la información más importante.

----
# Eigenvalues and Eigenvectors

Antes de profundizar en la evuación de los valores propios, es importante comprender qué son los vectores propios y los valores propios, así como su importancia en el álgebra lineal y el aprendizaje automático.

Un vector propio es un vector espacial que mantiene la misma dirección cuando se la aplica una transformación lineal (Como la multiplicación por una matriz). Matemáticamente, si `A` es una matriz cuadrada y `v` es un vector distinto de cero, entonces `v` es un vector propio de `A` si:

$$
A * v = λ * v
$$


Aquí, `λ` (*Lambda*) es el valor propio asociado al vector propio `v`.

El valor propio `λ` representa el facto escalar por el que se escala el vector propio `v` durante la transformación lineal. En otras palabras, cuando se multiplica la matriz `A` por su vector propio `v`, el resultado es un vector que apunta en la misma dirección que `v`, pero que se alarga o se acorta en un factor de `λ`.

![[Pasted image 20260922064934.png]]

Imaginemos una goma elástrica estirada a lo largo de un sistema de coordenadas. Podemos representar la goma elástica mediante un vector y transofrmarla utilizado una matriz.

Supongamos que, incialmente, la goma elástica está alineada con el eje x y tiene una longitud de 1 unidad. Esto se puede representar mediante el vector `v = [1, 0]`.

Ahora, imaginemos que aplicamos una transformación lineal (Estirándola) representada por la matriz `A`:

```python
A = [[2, 0],
     [0, 1]]
```

Cuando multiplicamos la matriz `A` por el vector `v`, obtendriamos:

```python
A * v = [[2, 0],
         [0, 1]] * [1, 0] = [2, 0]
```

El vector resultante es `[2, 0]`, que apunta en la misma dirección que el vector original `v`, pero se ha estirado un factor de 2. El vector propio es `v = [1, 0]`, y el valor propio correspondiente es `λ = 2`. 

-----
# The Eigenvalue Equation in PCA

En el **Principal Component Analysis** (*PCA*), la ecuación de los valores propios ayuda a identificar los componentes principales de los datos. Los componentes principales se obtienen resolviendo la siguiente eccuación de los valores propios:

$$
C * v = λ * v
$$

Donde:

 - `c` es la matriz de covarianza de los datos estandarizados. Esta matriz representa las relaciones entre las diferentes características, y cada elemento indica la covarianza entre dos características.
 - `v` es el vector propio. Los vectores propios representa las direccciones de los componentes principales en el espacio de características, indicando las direcciones de máxima varianza en los datos.
 - `λ` es el valor propio. Los valores propios representa la cantidad de varianza e xplicada por cada vector propio correspondiente (Componente principal). Los valores propios más grandes corresponden a vectores propios que capturan más varianza.

----
# Solving the Eigenvalue Equation

Para resolver esta ecuación hay que hallar los vectores propios y los valores propios de la matriz de covarianza. Esto se puede hacer utilizando técnicas como:

- **Eigenvalue Decomposition**: Cálculo directo de los valores propios y los vectores propios.
- **Singular Value Decomposition** (*SVD*): Un método numéricamente más estable que descompone la matriz de datos en vectores singualres y valores singulares relacionados con los vectores propios y los valores propios de la matriz de covarianza.

---
# Selecting Principal Components

Una vez hallados los vectores propios y los valores propios, se ordenan en orden descendente según sus valores propios correspondientes. Se seleccionan los `k` primeros vectores propios (Los que tienen los valores propios más grandes) para formar el nuevo espacio de características. Estos `k` primeros vectores propios representan los componentes principales que capturan la varianza más significativa de los datos.

La transformación de los datos originales `X` en la representación de menor dimensión `Y` puede expresarse como:

$$
Y = X * V
$$

Donde:

- `Y` es la matriz de datos transformada en el espacio de menor dimensión.
- `X` es la matriz de datos original.
- `V` es la matriz de los vectores propios seleccionados.

Esta transformación proyecta los puntos de datos orginales en el nuevo espacio de características definido por los componentes principales, lo que da como resultado una representación de menor dimensión que capta la varianza más significativa de los datos. Esta representación reducida puede utilizarse para diversos fines, como la visualización, la reducción del ruido y la mejora del rendimiento de los modelos de aprendizaje automático.

----
# Choosing the Number of Components

El número de componentes principales que se van a conservar es una decisión crucial en el *análisis de componentes principales* (*PCA*). Determina el equilibrio entre la reducción de la dimensionalidad y la conversación de la información.

Un enfoque habitual consiste en representar gráficamente la razón de varianza explicada en función del número de componenotes. La razón de varianza explicada indica la proporción de la varianza total capturada por cada componente principal. Al examinar el gráfico, se puede elegir el número de componentes que capturen un porcentaje suficientemente alto de la varianza total (Por ejemplo, el 95%). De este modo se garantiza que la representación reducida conserve la mayor parte de la información esencial de los datos originales.

----
# Data Assumptions

El **PCA** parte de ciertas hipótesis sobre los datos:

- **Linearity**: La linealidad supone que las relaciones entre las características son lineales.
- **Correlation**:Funciona mejor cuando existe una correlación significativa entre las características.
- **Scale**: Es sensible a la escala de las caracaterísticas, por lo que es imporatnte normalizar los datos antes de aplical el PCA.

El PCA es una técnica potente para la reducción de dimensionalidad y el análisis de datos. Permite simplificar conjuntos de datos complejos, extraer características significativas y visualizar los datos en un espacio de menor dimensionalidad. Sin embargo, es importante conocer sus supuestos y limitaciones para garantizar una aplicación eficaz y adecuada.